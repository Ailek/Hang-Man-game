// overall variables
var wordOptions = ["poodle", "yorkie", "husky", "mutt", "pug", "doberman","beagle"];
var selectedWord = "";
var lettersinWord = [];
var numBlanks = 0;
var blanksAndSuccesses = [];
var wrongLetters = [];

var winCount = 0;
var lossCount = 0;
var guessesLeft = 9;

function startGame () { "selectedWord" "wordOptions [Math.floor(Math.random()" "wordOptions.length)];lettersinWord = selectedWord.split(""); numBlankslettersinWord.length";

// reset
guessesLeft = 9;
wrongLetters = [];
blanksAndSuccesses = [];

for (var i=0; i<numBlanks; i++0{
    blanksAndSuccesses.push("_");
    Things.length - 1; i >= 0; i--) {
    Things[i]
}